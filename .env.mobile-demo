# Public build flags only. Offline demo with the bundled catalog: no API, no data collection.
# Used by the Playwright mobile tests and the CI demo build (npm run build:mobile:demo).
VITE_STATIC_PREVIEW=true
VITE_NATIVE_APP=true
VITE_BASE_PATH=/
