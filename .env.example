# Set NODE_ENV=production in the deployment environment, not in this shared file.
PORT=3101
APP_ORIGIN=http://localhost:5174
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=atelier
DB_PASSWORD=change-me
DB_NAME=atelier_de_vise
# Only for scripts that create/update the administrator; never exposed to the browser.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=choose-a-password-of-at-least-12-characters
